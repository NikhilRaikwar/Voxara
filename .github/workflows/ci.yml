name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Enable Corepack
        run: corepack enable

      - name: Use Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        # CI only typechecks and runs Vitest; native build lifecycle scripts are
        # unnecessary here and are intentionally blocked by workspace policy.
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Typecheck workspace
        run: pnpm run typecheck

      - name: Run API tests
        run: pnpm --filter @workspace/api-server run test
